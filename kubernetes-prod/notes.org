#+title: Notes

* Algemeine Notizen

** Installation
*** Systemvoraussetzungen
- Certain ports open
  check required ports
  #+begin_src shell
  nc 127.0.0.1 6443 -v
  #+end_src
- Unique hostname, mac address prod_uuid
- Disabled swap
  #+begin_src shell
  sudo swapoff -a
  #+end_src
*** Install container runtime
- Enable IPv4 packet forwarding
  #+begin_src shell
 sysctl params required by setup, params persist across reboots
        cat <<EOF | sudo tee /etc/sysctl.d/k8s.conf
        net.ipv4.ip_forward = 1
        EOF

        # Apply sysctl params without reboot
        sudo sysctl --system

  #+end_src
** Test auf ausfahle rigtige Dy urchfuehrung.
