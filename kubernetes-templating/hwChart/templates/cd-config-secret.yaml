apiVersion: v1
kind: Secret
metadata:
  name: { { include "hmChart.name" . } }
  namespace: homework
  annotations:
    kubernetes.io/service-account.name: cd
type: kubernetes.io/service-account-token
