apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hwChart.fullname" . }}-test-connection"
  labels:
    {{- include "hwChart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hwChart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
